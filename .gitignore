node_modules/
.history/
*.log
out.json
.DS_Store
Thumbs.db

# Real account save data pulled for saveImport.js mapping verification -- kept locally for
# future regression testing (see bridge/verify-save-mapping.mjs) but never committed.
bridge/test-fixtures/

# Optimizer benchmark output -- regenerated by tools/bench/run.js on every sweep.
tools/bench/results.json
tools/bench/results/

# TypeScript checker (devDependency only -- no build step, see CLAUDE.md)
package-lock.json

# Extracted game assets / APK working files -- large, and not ours to redistribute.
tools/save/upgrade-names.json
# Pulled game files (APKs ~235MB, IL2CPP metadata, real save data). See tools/gamefiles/README.md,
# which IS committed -- it documents what lives here and how to re-pull it.
tools/gamefiles/*
!tools/gamefiles/README.md

# Built bridge installers -- release artifacts, rebuilt from source by CI.
bridge/dist/
bridge/installer/*/.build/
